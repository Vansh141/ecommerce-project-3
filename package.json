{
  "name": "touch-ecommerce",
  "version": "1.0.0",
  "private": true,
  "description": "TOUCH — fashion e-commerce storefront and API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",

    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",

    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",

    "test": "npm --prefix backend test",
    "lint": "npm --prefix frontend run lint",
    "audit": "npm --prefix backend audit && npm --prefix frontend audit",

    "seed": "npm --prefix backend run seed",
    "seed:admin": "npm --prefix backend run seed:admin",
    "indexes": "npm --prefix backend run indexes",

    "verify": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
